Quick app.js to prove out an issue that was introduced into Titanium Mobile
around 3.1 or so.

Basically, when you open a smaller window over a larger one, accessibility
information doesn't flow through the smaller window like it should.  Instead,
the smaller window just covers the whole screen.
